package fr.uga.im2ag.m1info.chatservice.server;

abstract class ServerPacketHandler {
        public TchatsAppServer server;

        // abstract public void handle(ProtocolMessage protocolMessage);
        //
        // abstract public boolean canHandle(MessageType messageType);
        // ServerPacketHandler(TchatsAppServer server){}
    }
